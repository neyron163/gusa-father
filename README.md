## Settings

Before start app, you should pass env: `BOT_TOKEN` and `OPENAI_API_KEY`
Pass it before start app:

Copy file: `.env.dist`

```bash
cp .env.dist .env
```

```
BOT_TOKEN=#########
OPENAI_API_KEY=#####
```
